#!/bin/bash

LOCAL_SETTINGS_FILE=$(source ${CONTAINER_SCRIPTS_ROOT}/setup/setup.sh ${BASH_SOURCE} ${LOCAL_SETTINGS_FILE})

# ELASTIC_CONT_NAME
ELASTIC_CONT_NAME="elastic_cont"

echo "ELASTIC_CONT_NAME=${ELASTIC_CONT_NAME}" >> ${LOCAL_SETTINGS_FILE}

# ELASTIC_IMAGE
ELASTIC_IMAGE=$(get_tag)

echo "ELASTIC_IMAGE=${ELASTIC_IMAGE}" >> ${LOCAL_SETTINGS_FILE}